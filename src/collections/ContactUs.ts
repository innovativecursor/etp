// payload/collections/contactusform.ts

import { CollectionConfig } from 'payload'

const ContactUsForm: CollectionConfig = {
  slug: 'contactusform',
  admin: {
    useAsTitle: 'name',
  },
  access: {
    read: () => true,
    create: () => true,
  },
  fields: [
    {
      name: 'name',
      type: 'text',
      required: true,
    },
    {
      name: 'email',
      type: 'email',
      required: true,
    },
    {
      name: 'message',
      type: 'textarea',
      required: true,
    },
  ],
}

export default ContactUsForm
